Pushy Dialer Application
------------

1.To create the project settings, type:

  android update project --name RailTiffin Dialer -p . --target Google Inc.:Google APIs:22

2.To build the app, type:

  ant clean debug

3.To install the app, type

  ant installd
